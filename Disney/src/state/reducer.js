const initialState = {
    loading: false,
    error: "",
    success: {}
};

const reducer = ( state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}